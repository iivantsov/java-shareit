server.port: 8080
shareit-server.url: http://localhost:9090

logging:
  level:
    httpclient:
      wire: debug
    org:
      springframework:
        web:
          client.RestTemplate: debug
      apache:
        http: debug